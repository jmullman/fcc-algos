for (var i = 0; i < size; i++) {
  // twoDiArr.push(arr.slice(0,size));
  if (arr.length <= 0) {
    return twoDiArr;
  } else {
    twoDiArr.push(arr.slice(0,size));
    arr.splice(0,function (){
      if (size <= arr.length){
        return arr.length;
      } else {
        return size;
      }
    });
  }
}

Intent:
create top-level array
cut original array into the chunks of chunkSize
  if the last chunk is less than the size of chunkSize, get what you can and store it
store each chunk in its own array (? could go straight to the top-level array) Do **not** store empty arrays
add new chunks into the top-level array
return
